node_modules/
dist/
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

# never commit live OAuth tokens, wherever they land
**/.trakt_token.json

# generated-data backups (kept out of client/public so they never ship)
data-backups/

# sharded client payload — a build artifact derived from client/public/data.json
# by script/data-shards.ts on every dev start and build. Never edit by hand.
client/public/data/

*.log
.DS_Store

# local wrangler dev vars (LOCAL_DEV bypass)
.dev.vars
.wrangler/
